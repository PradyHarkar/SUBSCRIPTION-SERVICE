{
  "name": "subscription-service",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start", // Local development for React
    "build": "react-scripts build", // Build the React frontend
    "test": "react-scripts test", // Testing React app
    "eject": "react-scripts eject" // Optional: For customizing the React app build process
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "A subscription service with login and subscription features.",
  "dependencies": {
    "react": "^18.0.0", // Core React library
    "react-dom": "^18.0.0", // React DOM rendering
    "react-scripts": "^5.0.1", // For building and running React apps
    "stripe": "^18.0.0" // For subscription integration later
  },
  "devDependencies": {}
}